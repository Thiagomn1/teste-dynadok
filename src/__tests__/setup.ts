// Jest setup file
// Add global test configuration here if needed

// Suppress console.log/error during tests (optional)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   error: jest.fn(),
// };
